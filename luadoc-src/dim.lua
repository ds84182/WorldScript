--- The dim(ension) API gives access to worlds via dimesion ids
dim = {}

--- Gets the WorldObject at dimension id
-- @param id The dimension id, 0 is the overworld, -1 is the nether, and 1 is the end.
-- @return A worldobject
-- @see WorldObject
function dim.get(id) end
